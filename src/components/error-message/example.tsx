import { ErrorMessage } from '.'

const Example = () => {
  return (
    <ErrorMessage>
      This is an error message to be displayed when your fields are invalid or
      not satisfying the requirements
    </ErrorMessage>
  )
}

export { Example }
